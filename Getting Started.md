Leaderboard Service App [WIP]
=================================
Requirements
---
* Maven
* Java 21

Build and run locally
---
From the root directory run the command:
```
mvn clean install
```
Change directory to leaderboard-api
```
mvn spring-boot:run
```